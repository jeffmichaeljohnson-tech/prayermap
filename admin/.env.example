# ============================================================================
# PrayerMap Admin Dashboard - Environment Variables
# ============================================================================
#
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control
#
# Note: The admin dashboard uses the same backend as the main app
#
# ============================================================================

# ----------------------------------------------------------------------------
# REQUIRED: Supabase Configuration
# ----------------------------------------------------------------------------
# Must match the main app's Supabase project
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Your Supabase project URL (same as main app)
VITE_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anonymous/public key (same as main app)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ----------------------------------------------------------------------------
# OPTIONAL: Admin Authentication
# ----------------------------------------------------------------------------
# Credentials for automated testing
# Security: NEVER use production admin credentials here

# Admin email for E2E tests
ADMIN_EMAIL=admin@example.com

# Admin password for E2E tests
ADMIN_PASSWORD=test-password-here

# ============================================================================
# Setup Instructions
# ============================================================================
#
# 1. Copy this file:
#    cd admin && cp .env.example .env.local
#
# 2. Copy the same Supabase credentials from the main app:
#    - The admin dashboard connects to the same Supabase project
#    - Use the same VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY
#
# 3. The admin dashboard runs on port 5174 (main app uses 5173):
#    cd admin && npm run dev
#
# 4. Access the admin dashboard:
#    http://localhost:5174
#
# ============================================================================
# Notes
# ============================================================================
#
# - The admin dashboard shares the same Supabase backend as the main app
# - Admin permissions are controlled by RLS policies in the database
# - Use the admin dashboard to moderate content, manage users, etc.
# - The admin dashboard is deployed separately from the main app
#
# ============================================================================
